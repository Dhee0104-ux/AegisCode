services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    volumes: ["./backend/projects:/app/projects"]
    environment:
      - CORS_ORIGINS=http://localhost:5173
    depends_on: []
  frontend:
    build: ./frontend
    ports: ["5173:5173"]
    depends_on: [backend]
