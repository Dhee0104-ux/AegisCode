# API
POST /api/projects — create and start a project build.
GET /api/projects — list projects.
GET /api/projects/{project_id} — retrieve project state.
GET /api/projects/{project_id}/audit — retrieve audit trail.
WS /api/ws/projects/{project_id} — receive live agent events.
GET /api/health — health check.
